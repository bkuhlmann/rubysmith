#! /usr/bin/env bash

set -o nounset
set -o errexit
set -o pipefail
IFS=$'\n\t'

read -r version < ".ruby-version"

docker buildx \
       build \
       --load \
       --build-arg RUBY_VERSION="${version}" \
       --tag <%= settings.project_name %>:latest \
       "$(pwd)"
